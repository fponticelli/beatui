/**
 * JSON Schema form generation for `@tempots/beatui/json-schema`.
 *
 * Automatically generates validated form UIs from JSON Schema definitions.
 * Supports custom widget registration, nested objects/arrays, conditional
 * schemas, and integrates with BeatUI's form controller system.
 *
 * ```ts
 * import { JsonSchemaForm } from '@tempots/beatui/json-schema'
 * ```
 *
 * @module
 */

export * from '../components/json-schema'
