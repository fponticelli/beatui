/**
 * Public entry point for the `@tempots/beatui/prosemirror` optional module.
 *
 * Re-exports the ProseMirror markdown editor component and its toolbar so
 * consumers can import them from a single path:
 *
 * ```ts
 * import { ProseMirrorMarkdownInput, ProseMirrorToolbar } from '@tempots/beatui/prosemirror'
 * ```
 *
 * @module
 */

export * from '../components/prosemirror/prosemirror-markdown-input'
export * from '../components/prosemirror/prosemirror-toolbar'
