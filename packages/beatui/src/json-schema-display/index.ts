/**
 * Read-only JSON Schema display for `@tempots/beatui/json-schema-display`.
 *
 * Renders JSON Schema structures as formatted, non-editable documentation.
 * Useful for API reference pages, schema explorers, and data dictionaries.
 *
 * ```ts
 * import { JsonSchemaDisplay } from '@tempots/beatui/json-schema-display'
 * ```
 *
 * @module
 */

export * from '../components/json-schema-display'
