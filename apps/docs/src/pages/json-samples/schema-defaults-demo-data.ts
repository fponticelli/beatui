/**
 * Schema Defaults Demo - Initial Data
 *
 * This is intentionally empty to demonstrate that the form will be
 * populated with default values from the schema when applySchemaDefaults
 * is enabled (the default behavior).
 */
export default {}
